# Dictionary term-list variant

Builds an external term dictionary from `dev_v2` and applies it to `dev_v1` as a fourth term-list variant (alongside `original`/`expand`/`cleaned`), then compares GPT-4o-mini's performance on it against the other variants.

This is a **thin wrapper**: no experiment-local results, only `scripts/`, `data/`, and `report/`. It reads the shared `results/` tree, and owns the two scripts that build the dictionary variant compared here (`scripts/build_term_dictionary.py`, `scripts/apply_dictionary_to_dev_v1.py`).

`data/dev_v2_dictionary/` (the term dictionary `build_term_dictionary.py` builds from dev_v2, then `apply_dictionary_to_dev_v1.py` applies to dev_v1) lives here rather than in the shared `data/` tree since this experiment is its only consumer — analogous to `experiments/term_expansion/gpt_proposed_terms/data/`. It doesn't exist on disk; `data/dev_v1/dev_v1_dictionary/` (its already-applied output) does, so the report tables below that use it work today — regenerating `data/dev_v2_dictionary/` itself is only needed if you want to rebuild `data/dev_v1/dev_v1_dictionary/` from scratch (`python experiments/term_expansion/dictionary/scripts/build_term_dictionary.py --all`, then `apply_dictionary_to_dev_v1.py`).

## Report tables

Comparison of `dev_v1/original` vs. `dev_v1/dictionary`, generated by [`scripts/compare_v1_variants_to_excel.py`](scripts/compare_v1_variants_to_excel.py):

| File | Regenerate |
| ---- | ---------- |
| `report/dev_v1_original_vs_dev_v1_dictionary_gpt_comparison.xlsx` | `python experiments/term_expansion/dictionary/scripts/compare_v1_variants_to_excel.py` |

`proper_term`-only comparison across all 4 dev_v1 term-list variants (`original`/`expand`/`cleaned`/`dictionary`) for GPT — the only model with dictionary results (`qwen_3b`/`qwen_7b` have no `results/dev_v1/dictionary/` data), generated by [`scripts/compare_proper_term_across_expansion_modes_to_excel.py`](scripts/compare_proper_term_across_expansion_modes_to_excel.py):

| File | Regenerate |
| ---- | ---------- |
| `report/dev_v1_proper_term_across_expansion_modes.xlsx` | `python experiments/term_expansion/dictionary/scripts/compare_proper_term_across_expansion_modes_to_excel.py` |

For the dev_v1-vs-dev_v2 comparability check (a different axis — test set vs. training set, not term-list variant), see [`dataset_comparison/`](../../dataset_comparison/README.md).
